version: "1.0"
description: "JDBC MySQL"

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/gc_coffee?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: password

  sql:
    init:
      platform: mysql
      mode: always
